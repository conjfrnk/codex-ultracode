# Codex Conductor: project spec (moved)

The canonical project spec is [`PROJECT_SPEC.md`](PROJECT_SPEC.md).

This file used to be a byte-identical duplicate of `PROJECT_SPEC.md`, which created drift risk: an edit made to only one copy would silently desynchronize "the spec" with no tooling to detect it. It is kept as a short pointer, not a second copy, so there is exactly one source of truth.
