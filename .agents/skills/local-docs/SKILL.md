---
name: local-docs
description: Provide one deterministic Skill-local MCP tool for isolation verification.
---

Use the `localDocs.prove_isolation` tool only when a workflow explicitly selects this Skill and route.
