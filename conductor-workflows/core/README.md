# Core workflow fixtures

These dependency-free examples are generated by `conductor-runtime init` and
are executable compatibility fixtures for `conductor.core.workflow.v1`.

```sh
python3 -m conductor_runtime schema
python3 -m conductor_runtime init review.json --template read-only
python3 -m conductor_runtime init change.json --template staged-write
```
