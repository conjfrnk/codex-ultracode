# Extras workflow fixtures

These examples target the optional `conductor-extras` runtime and its extended
`conductor.workflow.v1` dialect. They are not accepted by the dependency-free
core runtime. The root-level copies are deprecated compatibility paths.
