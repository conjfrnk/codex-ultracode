interface:
  display_name: "Debug from Evidence"
  short_description: "Trace root causes before changing code"
  default_prompt: "Use $debug-from-evidence to reproduce this failure, prove the cause, and make the smallest justified fix."
policy:
  allow_implicit_invocation: true
